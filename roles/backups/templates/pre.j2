#!/bin/sh
# this file is generated by Ansible.
# You can replace the override this file from being used
# in the Ansible playbook by placing a Jinja2 template file
# similar to this one in a directory, and defining the path to
# that directory in the variable duply_prep_directory. 

{% if 'mail_server' in group_names %}

mkdir /tmp/postgres
chown postgres:postgres /tmp/postgres
chmod 700 /tmp/postgres

echo "Running pg_dumpall..."
su - -c "pg_dumpall > /tmp/postgres/pg_dumpall.{{ ansible_fqdn }}.psql" postgres

{% endif %}

exit 0
